# 快速测试指南

## 🎯 如何测试 AI 智能调度

### 前置条件

确保你已经完成以下步骤：

1. ✅ 数据库已初始化
   ```bash
   pnpm db:push
   ```

2. ✅ 环境变量已配置
   ```bash
   # .env 文件中
   DATABASE_URL=postgresql://...
   ENCRYPTION_KEY=...
   ```

3. ✅ 开发服务器已启动
   ```bash
   pnpm dev
   ```

---

## 🧪 测试场景

### 场景 1: 明确的添加指令

**测试输入**:
```
帮我在 example.com 添加一个指向 1.2.3.4 的 A 记录
```

**预期结果**:
- ✅ AI 解析成功
- ✅ 显示操作卡片:
  - 操作类型: CREATE
  - 域名: example.com
  - 记录类型: A
  - 记录名称: @
  - 记录值: 1.2.3.4
  - TTL: 600秒
- ✅ reasoning: "用户明确要求在 example.com 添加 A 记录指向 1.2.3.4，未指定子域名，因此使用根域名 (@)，TTL 使用默认值 600 秒"

**注意**: 需要先添加服务商并同步域名才能执行操作

---

### 场景 2: 更新已有记录

**测试输入**:
```
把 example.com 的 www 记录改成 8.8.8.8
```

**预期结果**:
- ✅ AI 解析成功
- ✅ 显示操作卡片:
  - 操作类型: UPDATE
  - 域名: example.com
  - 记录类型: A
  - 记录名称: www
  - 记录值: 8.8.8.8
- ✅ reasoning: "用户要求修改 www.example.com 的记录，根据 8.8.8.8 是 IPv4 地址判断为 A 记录，UPDATE 操作会更新现有的 www A 记录的值为 8.8.8.8"

---

### 场景 3: 删除记录

**测试输入**:
```
删除 example.com 的 TXT 记录
```

**预期结果**:
- ✅ AI 解析成功
- ✅ 显示操作卡片:
  - 操作类型: DELETE
  - 域名: example.com
  - 记录类型: TXT
  - 记录名称: @
- ✅ reasoning: "用户要求删除 TXT 记录，未指定具体名称，推测是删除根域名的 TXT 记录（如 SPF 记录）。注意：如果有多个 TXT 记录，可能需要进一步确认"

**注意**: 需要二次确认才会执行

---

### 场景 4: 模糊指令 - 需要澄清

**测试输入**:
```
帮我添加一个记录
```

**预期结果**:
- ✅ AI 要求澄清
- ✅ 显示提示信息:
  - 消息: "我需要更多信息来帮你添加 DNS 记录"
  - reasoning: "用户的指令过于模糊，缺少以下关键信息：1. 目标域名（如 example.com）2. 记录类型（A/CNAME/TXT 等）3. 记录值（IP 地址或目标域名）"
  - 建议:
    - "帮我在 example.com 添加一个指向 1.2.3.4 的 A 记录"
    - "在 test.com 添加 www 指向 api.example.com 的 CNAME 记录"

---

### 场景 5: CNAME 记录

**测试输入**:
```
给 blog.example.com 添加 CNAME 指向 example.com
```

**预期结果**:
- ✅ AI 解析成功
- ✅ 显示操作卡片:
  - 操作类型: CREATE
  - 域名: example.com
  - 记录类型: CNAME
  - 记录名称: blog
  - 记录值: example.com
- ✅ reasoning: "用户明确要求添加 CNAME 记录，blog.example.com 指向 example.com，CNAME 记录的 name 是 'blog'，content 是目标域名 'example.com'"

---

### 场景 6: MX 记录

**测试输入**:
```
为 example.com 配置邮件服务器，优先级 10，指向 mail.example.com
```

**预期结果**:
- ✅ AI 解析成功
- ✅ 显示操作卡片:
  - 操作类型: CREATE
  - 域名: example.com
  - 记录类型: MX
  - 记录名称: @
  - 记录值: mail.example.com
  - TTL: 600
  - 优先级: 10
- ✅ reasoning: "用户要求配置邮件服务器，这是 MX 记录，优先级明确指定为 10，内容是邮件服务器域名 mail.example.com"

---

## 🔍 端到端测试流程

### 步骤 1: 添加服务商

1. 访问 http://localhost:5000/providers
2. 点击"添加服务商"
3. 填写信息:
   - 名称: Cloudflare
   - 类型: Cloudflare
   - API Token: [你的 Cloudflare API Token]
4. 点击"测试连接"
5. 保存

### 步骤 2: 同步域名

1. 在服务商列表中，找到刚添加的服务商
2. 点击"同步域名"
3. 等待同步完成

### 步骤 3: 测试 AI 调度

1. 访问 http://localhost:5000
2. 在 AI Magic Box 中输入指令:
   ```
   帮我在 example.com 添加一个指向 1.2.3.4 的 A 记录
   ```
3. 点击"解析指令"
4. 查看解析结果
5. 点击"确认执行"
6. 查看执行结果

---

## 📊 性能指标

### 预期响应时间

- AI 解析: 1-3 秒（取决于网络和模型）
- 数据库查询: < 100ms
- Provider API 调用: 500ms-2s（取决于服务商）

### Token 消耗

- 平均每次解析: 200-500 tokens
- 包含 reasoning: 300-600 tokens

---

## 🐛 常见问题

### Q1: AI 解析失败

**可能原因**:
- 网络问题
- API Key 配置错误
- 指令过于复杂

**解决方案**:
- 检查网络连接
- 验证 API Key 是否正确
- 尝试简化指令

### Q2: 执行操作失败

**可能原因**:
- 域名不存在
- 凭证失效
- 服务商 API 限制

**解决方案**:
- 先同步域名
- 测试服务商连接
- 检查服务商配额

### Q3: AI 理解错误

**可能原因**:
- 指令表达不清
- 缺少关键信息
- 使用了不常见的表达

**解决方案**:
- 使用更明确的表达
- 参考 AI 提供的建议
- 查看 System Prompt 设计

---

## 📝 测试报告模板

| 测试场景 | 指令 | AI 解析 | 执行结果 | 备注 |
|---------|-------|---------|---------|------|
| 添加 A 记录 | 帮我在 example.com 添加一个指向 1.2.3.4 的 A 记录 | ✅ 成功 | ⏳ 待执行 | 需要先配置域名 |
| 更新记录 | 把 example.com 的 www 记录改成 8.8.8.8 | ✅ 成功 | ⏳ 待执行 | - |
| 删除记录 | 删除 example.com 的 TXT 记录 | ✅ 成功 | ⏳ 待执行 | - |
| 模糊指令 | 帮我添加一个记录 | ✅ 需要澄清 | - | AI 正确要求澄清 |

---

## 🎓 最佳实践

### 编写清晰指令

**好的指令**:
- ✅ "帮我在 example.com 添加一个指向 1.2.3.4 的 A 记录"
- ✅ "把 www.example.com 的 CNAME 记录改成 api.example.com"
- ✅ "删除 test.com 的所有 TXT 记录"

**模糊的指令**:
- ❌ "添加一个记录"
- ❌ "删除那个记录"
- ❌ "修改域名"

### 验证 AI 理解

- 仔细阅读 reasoning 字段
- 检查操作类型和参数是否正确
- 如有疑问，重新表述指令

### 安全操作

- DELETE 操作前仔细检查
- 先用 QUERY 查看现有记录
- 备份重要记录

---

**开始测试吧！** 🚀
